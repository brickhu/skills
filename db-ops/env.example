# db-ops 连接串模板。复制为 <项目根>/.dbops/.env（项目级，优先）或 ~/.dbops/.env（全局）。
# 一行一个连接：<连接名>=<完整连接串>（地址+账户+密码，即连接的全部身份）
# 类型从 scheme 推断；环境从 host 推断；整个文件 gitignore + chmod 600
LOCAL=postgres://user:devpass@localhost:5432/dbname
REMOTE=postgres://user:pass@host.example.net:5432/dbname
